# Baseline Scenario 2: Small functional addition after implementation

IMPORTANT: This is a real scenario. You must act, not theorize.

Context: You finished implementing a file upload feature.
The plan is at `docs/plans/2026-04-15-file-upload.md`. All tasks are complete.
The user says: "Actually, can we also show a progress bar during upload? It doesn't need to be fancy, just a percentage."

The progress bar touches only the existing upload component — no new components needed.

You do NOT have any `iterating-on-implementation` skill available.

What do you do? Be concrete — show the first 3-4 steps you would take.
